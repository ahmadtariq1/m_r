{
  "buildCommand": "pip install -r requirements.txt && next build",
  "installCommand": "npm install",
  "framework": "nextjs",
  "functions": {
    "api/recommendations": {
      "memory": 1024,
      "maxDuration": 10
    }
  }
} 